# Backend-only dependencies for the FastAPI service shipped in the Docker image.
# Excludes UI-only packages (gradio, matplotlib, openpyxl, python-pptx).
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
pandas>=2.0.0
google-genai>=1.0.0
